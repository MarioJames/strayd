[package]
name = "port-deck-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
port-deck-core = { path = "../port-deck-core" }
serde = { version = "1", features = ["derive"] }
netstat2 = "0.11.2"
sysinfo = "0.39.6"
thiserror = "2"
